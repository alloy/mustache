Kicker.silent = true

recipe :ruby

process do |files|
  tests = files.take_and_map do |file|
    # Run all tests if anything in lib changes.
    Dir.glob('test/**/*_test.rb') if file =~ /^lib\//
  end
  
  Ruby.run_tests(tests)
end